# HALO Factory — Python dependencies (Pass 2)
# Core web framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0

# HTTP client (Kernel gateway proxy)
httpx>=0.27.0

# Redis (Streams + Pub/Sub)
redis>=5.0
aioredis>=2.0

# MinIO artifact storage
minio>=7.2

# tree-sitter (TDAD AST parsing)
tree-sitter>=0.25.0
tree-sitter-python>=0.25.0
tree-sitter-javascript>=0.25.0
tree-sitter-typescript>=0.23.0

# RAG embeddings
sentence-transformers>=3.0

# Filesystem watcher (supervisor inotify)
watchdog>=4.0

# YAML (spec frontmatter parsing)
pyyaml>=6.0

# Testing
httpx  # also used by starlette.testclient